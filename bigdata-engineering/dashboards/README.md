In addition to the PDF export, this repository includes the Superset
dashboard configuration files (`.yaml`) under `dashboards/superset_export/`,
allowing the dashboard to be versioned and re-imported if needed.
